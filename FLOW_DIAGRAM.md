# 🎯 Owner Flow - Visual Diagram

## Complete User Journey

```
┌─────────────────────────────────────────────────────────────────────┐
│                         OWNER JOURNEY MAP                           │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────────┐
│  NOT LOGGED IN   │
│  (Guest User)    │
└────────┬─────────┘
         │
         │ Tries to access /list-property or /owner/dashboard
         ↓
┌────────────────────────────────────────────────────────────────────┐
│  🔒 AUTHENTICATION GUARD                                           │
│  - requireRole(['OWNER']) check fails                              │
│  - Redirects to /login?type=owner                                  │
└────────┬───────────────────────────────────────────────────────────┘
         │
         ↓
┌────────────────────────────────────────────────────────────────────┐
│  🔐 LOGIN PAGE (/login?type=owner)                                 │
│  - Enter phone/email + password                                    │
│  - Click "Login" button                                            │
└────────┬───────────────────────────────────────────────────────────┘
         │
         │ POST /api/v1/owner/auth/login
         ↓
┌────────────────────────────────────────────────────────────────────┐
│  🎟️ JWT TOKEN GENERATED                                            │
│  - role: 'OWNER'                                                   │
│  - userId, email, phone, name                                      │
│  - Stored in localStorage                                          │
│  - apiClient.setToken(token)                                       │
└────────┬───────────────────────────────────────────────────────────┘
         │
         │ router.replace('/owner/dashboard')
         ↓
┌────────────────────────────────────────────────────────────────────┐
│  📊 OWNER DASHBOARD (/owner/dashboard)                             │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │  HEADER                                                       │ │
│  │  - Welcome Owner!                                             │ │
│  │  - [Quick Add] [Full Listing Form] buttons                   │ │
│  └──────────────────────────────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │  STATS CARDS (4)                                              │ │
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐   │ │
│  │  │Properties │ │ Inquiries │ │   Views   │ │ Pot. Rent │   │ │
│  │  │ 5 (3/2)   │ │  12 (3)   │ │    248    │ │ ₹85,000   │   │ │
│  │  └───────────┘ └───────────┘ └───────────┘ └───────────┘   │ │
│  └──────────────────────────────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │  SIDEBAR                    │  MAIN CONTENT AREA             │ │
│  │  • My Listings     ──────→  │  TAB 1: My Properties          │ │
│  │  • Leads & Inquiries        │  TAB 2: Leads & Inquiries      │ │
│  │  • Add Property             │                                 │ │
│  │  • My Profile               │                                 │ │
│  └──────────────────────────────────────────────────────────────┘ │
└────────┬───────────────────────────────────┬───────────────────────┘
         │                                   │
         │ Click "Add Property"              │ Click "My Properties" tab
         ↓                                   ↓
┌────────────────────────────┐    ┌──────────────────────────────────┐
│  MY PROPERTIES TAB         │    │  🏠 PROPERTY LISTING PAGE        │
│  ┌──────────────────────┐  │    │  (/list-property)                │
│  │  Property Card #1    │  │    │  ┌────────────────────────────┐ │
│  │  ┌────────────────┐  │  │    │  │  FORM SECTIONS             │ │
│  │  │ Property Image │  │  │    │  │  • Basic Info              │ │
│  │  └────────────────┘  │  │    │  │  • Location Details        │ │
│  │  Luxury 3BHK         │  │    │  │  • Pricing                 │ │
│  │  ₹35,000/month       │  │    │  │  • Property Details        │ │
│  │  Mumbai, Andheri     │  │    │  │  • Utilities               │ │
│  │  [ACTIVE] [Verified] │  │    │  │  • Amenities               │ │
│  │  [Edit][Pause][Del]  │  │    │  │  • Preferences             │ │
│  └──────────────────────┘  │    │  │  • Contact Details         │ │
│  ┌──────────────────────┐  │    │  └────────────────────────────┘ │
│  │  Property Card #2    │  │    │  ┌────────────────────────────┐ │
│  │  ...                 │  │    │  │  📱 MOBILE VERIFICATION    │ │
│  └──────────────────────┘  │    │  │  WhatsApp: [9876543210]   │ │
└────────────────────────────┘    │  │  [Send OTP] → Enter OTP    │ │
                                  │  │  [Verify] → ✅ Verified     │ │
                                  │  └────────────────────────────┘ │
                                  │  ┌────────────────────────────┐ │
                                  │  │  📸 IMAGE UPLOAD           │ │
                                  │  │  Upload 2-5 property images│ │
                                  │  │  (3/5 uploaded)            │ │
                                  │  └────────────────────────────┘ │
                                  │  [Publish Property] ──────────┐ │
                                  └───────────────────────────────┼─┘
                                                                  │
                                      POST /api/v1/owner/property │
                                      POST S3 presigned URLs      │
                                                                  ↓
                                  ┌───────────────────────────────────┐
                                  │  ✅ SUCCESS                        │
                                  │  - Property created                │
                                  │  - Images uploaded to S3           │
                                  │  - Toast: "Property published!"    │
                                  │  - Redirect to dashboard           │
                                  └───────────────┬───────────────────┘
                                                  │
                                                  ↓
┌────────────────────────────────────────────────────────────────────┐
│  📊 OWNER DASHBOARD (Refreshed)                                    │
│  - Stats updated (Total Properties: 6)                             │
│  - New property visible in "My Properties" tab                     │
│  - Can edit/pause/delete                                           │
└────────┬───────────────────────────────────────────────────────────┘
         │
         │ Click "Leads & Inquiries" tab
         ↓
┌────────────────────────────────────────────────────────────────────┐
│  📧 LEADS & INQUIRIES TAB                                          │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │  TABLE (8 columns)                                            │ │
│  │  ┌─────────┬────────┬────────┬─────────┬──────┬────────┬─────┤ │
│  │  │Property │ Seeker │Contact │ Message │ Type │ Status │ ... │ │
│  │  ├─────────┼────────┼────────┼─────────┼──────┼────────┼─────┤ │
│  │  │3BHK Apt │Rahul S │📞98765 │Looking..│INQUIR│[NEW]   │[Del]│ │
│  │  │2BHK Flat│Priya M │📞98123 │Intereste│INQUIR│[CONTACT│[Del]│ │
│  │  └─────────┴────────┴────────┴─────────┴──────┴────────┴─────┘ │
│  └──────────────────────────────────────────────────────────────┘ │
│  - Click phone number → Opens dialer                               │
│  - Click [Delete] → Remove inquiry                                 │
└────────┬───────────────────────────────────────────────────────────┘
         │
         │ Click "My Profile"
         ↓
┌────────────────────────────────────────────────────────────────────┐
│  👤 PROFILE PAGE (/profile)                                        │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │  Profile Header                                               │ │
│  │  - Owner Name                                                 │ │
│  │  - Account Type: OWNER                                        │ │
│  └──────────────────────────────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │  Profile Info                                                 │ │
│  │  - Email: owner@example.com                                   │ │
│  │  - Phone: +91 9876543210                                      │ │
│  │  - Verified: ✅ Yes                                            │ │
│  └──────────────────────────────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │  Actions                                                      │ │
│  │  [Edit Profile] [Change Password] [Logout]                   │ │
│  └──────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────┘
```

---

## Property Management Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                    PROPERTY MANAGEMENT ACTIONS                      │
└─────────────────────────────────────────────────────────────────────┘

Property Card on Dashboard:
┌────────────────────────────────────────────────────────┐
│  ┌─────────────────────┐                               │
│  │   Property Image    │                               │
│  └─────────────────────┘                               │
│  Luxury 3BHK Apartment                                 │
│  ₹35,000/month                                         │
│  Mumbai, Andheri West, Lokhandwala                     │
│  [ACTIVE] [Verified] [2 Inquiries]                     │
│                                                         │
│  [✏️ Edit] [👁️ Pause] [🗑️ Delete]                       │
└────┬────────────┬────────────┬──────────────────────────┘
     │            │            │
     │            │            └──────────────────┐
     │            │                               │
     │            │ Click Pause/Activate          │ Click Delete
     │            ↓                               ↓
     │   ┌───────────────────┐          ┌──────────────────┐
     │   │  PAUSE PROPERTY   │          │ DELETE PROPERTY  │
     │   │  PUT /property/id │          │ DELETE /property │
     │   │  status: PAUSED   │          │ Confirmation     │
     │   │  ✅ Success Toast  │          │ ✅ Success Toast  │
     │   └───────────────────┘          │ Removed from list│
     │                                  └──────────────────┘
     │
     │ Click Edit
     ↓
┌────────────────────────────────────────────────────────┐
│  EDIT PROPERTY DIALOG                                  │
│  ┌──────────────────────────────────────────────────┐ │
│  │  Title: [Luxury 3BHK Apartment        ]         │ │
│  │  Rent: [₹ 35000                       ]         │ │
│  │  Location: [Mumbai, Andheri           ]         │ │
│  │  Description: [Beautiful spacious...  ]         │ │
│  │  ...                                             │ │
│  │  [Cancel] [Save Changes]                        │ │
│  └──────────────────────────────────────────────────┘ │
│                   │                                    │
│                   │ Click Save Changes                 │
│                   ↓                                    │
│         PUT /api/v1/owner/property/:id                │
│         ✅ Success Toast: "Property updated"           │
│         Dashboard refreshes with new data             │
└────────────────────────────────────────────────────────┘
```

---

## Lead Management Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                      LEAD MANAGEMENT FLOW                           │
└─────────────────────────────────────────────────────────────────────┘

Leads & Inquiries Tab:
┌────────────────────────────────────────────────────────────────────┐
│  Lead Row in Table:                                                │
│  ┌──────────┬────────┬──────────┬──────────┬──────┬────────┬─────┐│
│  │3BHK Apt  │Rahul S │📞9876543 │Looking...│INQUIR│[NEW]   │[Del]││
│  └──────────┴────────┴─────┬────┴──────────┴──────┴────────┴──┬──┘│
│                            │                                  │    │
│                            │ Click Phone Number               │    │
│                            ↓                                  │    │
│                   ┌──────────────────┐                       │    │
│                   │ OPEN PHONE DIALER│                       │    │
│                   │ tel:+919876543210│                       │    │
│                   │ Call seeker      │                       │    │
│                   └──────────────────┘                       │    │
│                                                               │    │
│                                                    Click Delete    │
│                                                               ↓    │
│                                              ┌──────────────────┐  │
│                                              │ DELETE LEAD      │  │
│                                              │ DELETE /contact  │  │
│                                              │ ✅ Success Toast  │  │
│                                              │ Row removed      │  │
│                                              └──────────────────┘  │
└────────────────────────────────────────────────────────────────────┘
```

---

## API Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         API COMMUNICATION                           │
└─────────────────────────────────────────────────────────────────────┘

FRONTEND (Next.js)          BACKEND (Express)           DATABASE
─────────────────           ─────────────────           ─────────

Login Request
    │
    │ POST /api/v1/owner/auth/login
    │ { email, password }
    ├────────────────────────→  ownerAuth.login()
    │                               │
    │                               │ Query User table
    │                               ├──────────────→  SELECT * FROM User
    │                               │                 WHERE email = ?
    │                               │←─────────────   User data
    │                               │
    │                               │ Generate JWT
    │                               │ (role: 'OWNER')
    │←────────────────────────  { success, token, data }
    │ Store token
    │ Redirect to dashboard
    │

Property Creation
    │
    │ POST /api/v1/owner/property
    │ { title, type, rent, ... }
    ├────────────────────────→  ownerListing.createProperty()
    │                               │
    │                               │ Verify WhatsApp verified
    │                               ├──────────────→  SELECT * FROM
    │                               │                 TempMobileVerify
    │                               │←─────────────   { verified: true }
    │                               │
    │                               │ Create property
    │                               ├──────────────→  INSERT INTO Property
    │                               │←─────────────   Property created
    │←────────────────────────  { success, propertyId }
    │
    │ POST /api/v1/owner/property/:id/upload-images
    │ { imageCount: 5 }
    ├────────────────────────→  uploadImagesController()
    │                               │
    │                               │ Generate S3 presigned URLs
    │←────────────────────────  { presignedUrls: [...] }
    │
    │ PUT to S3 presigned URLs
    │ (Upload images directly to S3)
    ├────────────────────────→  AWS S3
    │←────────────────────────  Images uploaded
    │
    │ Update property with image URLs
    │ PUT /api/v1/owner/property/:id
    ├────────────────────────→  ownerListing.updateProperty()
    │                               │
    │                               │ Update image URLs
    │                               ├──────────────→  UPDATE Property
    │                               │                 SET images = [...]
    │                               │←─────────────   Updated
    │←────────────────────────  { success }
    │ Redirect to dashboard
    │

Fetch Dashboard Data
    │
    │ GET /api/v1/owner/property
    ├────────────────────────→  ownerListing.getOwnerProperties()
    │                               │
    │                               │ Query owner's properties
    │                               ├──────────────→  SELECT * FROM Property
    │                               │                 WHERE ownerId = ?
    │                               │←─────────────   Properties array
    │←────────────────────────  { success, properties: [...] }
    │
    │ GET /api/v1/contact/owner/contacts
    ├────────────────────────→  contactController.getOwnerContacts()
    │                               │
    │                               │ Query contacts/leads
    │                               ├──────────────→  SELECT * FROM Contact
    │                               │                 JOIN Property
    │                               │←─────────────   Contacts array
    │←────────────────────────  { success, contacts: [...] }
    │
    │ Render dashboard with data
    │
```

---

## State Management Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                    REACT STATE MANAGEMENT                           │
└─────────────────────────────────────────────────────────────────────┘

useOwnerData Hook:
┌────────────────────────────────────────────────────────────────────┐
│  const {                                                           │
│    properties,      // Property[] - All owner properties           │
│    contacts,        // Lead[] - All inquiries/leads               │
│    totalProperties, // number - Count of all properties           │
│    activeProperties,// number - Count of ACTIVE properties        │
│    draftProperties, // number - Count of DRAFT properties         │
│    views,           // number - Sum of all property views         │
│    leads,           // number - Count of all leads                │
│    newLeads,        // number - Count of NEW status leads         │
│    earnings,        // number - Sum of rent for ACTIVE properties │
│    isLoading,       // boolean - Loading state                    │
│    loadProperties,  // () => Promise<void> - Fetch properties     │
│    loadLeads,       // () => Promise<void> - Fetch leads          │
│    updateProperty,  // (id, data) => Promise<void> - Update       │
│    deleteProperty,  // (id) => Promise<void> - Delete             │
│    togglePropertyStatus, // (id) => Promise<void> - Pause/Activate│
│    deleteContact,   // (id) => Promise<void> - Delete lead        │
│  } = useOwnerData()                                                │
└────────────────────────────────────────────────────────────────────┘

Component Rendering:
┌────────────────────────────────────────────────────────────────────┐
│  OwnerDashboard Component                                          │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │  useEffect(() => {                                            │ │
│  │    loadProperties()  // Fetch on mount                        │ │
│  │    loadLeads()                                                │ │
│  │  }, [])                                                       │ │
│  └──────────────────────────────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │  Render Stats Cards                                           │ │
│  │  - totalProperties, activeProperties, draftProperties         │ │
│  │  - leads, newLeads                                            │ │
│  │  - views                                                      │ │
│  │  - earnings                                                   │ │
│  └──────────────────────────────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │  Render Property Cards                                        │ │
│  │  {properties.map(property => (                                │ │
│  │    <PropertyCard                                              │ │
│  │      onEdit={() => updateProperty(property.id, newData)}     │ │
│  │      onPause={() => togglePropertyStatus(property.id)}       │ │
│  │      onDelete={() => deleteProperty(property.id)}            │ │
│  │    />                                                         │ │
│  │  ))}                                                          │ │
│  └──────────────────────────────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │  Render Leads Table                                           │ │
│  │  {contacts.map(lead => (                                      │ │
│  │    <LeadRow                                                   │ │
│  │      onDelete={() => deleteContact(lead.id)}                 │ │
│  │    />                                                         │ │
│  │  ))}                                                          │ │
│  └──────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────┘
```

---

## Error Handling Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                        ERROR HANDLING                               │
└─────────────────────────────────────────────────────────────────────┘

API Call Error:
    │
    │ API Request
    ├──────────────────────→ Backend
    │                           │
    │                           ✗ Error occurred
    │←────────────────────── { success: false, error: "..." }
    │
    │ Catch error in frontend
    │
    ├─→ Display toast notification
    │   toast({
    │     title: "Error",
    │     description: error.message,
    │     variant: "destructive"
    │   })
    │
    ├─→ Set error state
    │   setError(error.message)
    │
    ├─→ Log to console (dev mode)
    │   console.error('[API Error]', error)
    │
    └─→ Keep UI functional
        - Don't crash app
        - Show fallback UI if needed
        - Allow retry

Authentication Error:
    │
    │ 401 Unauthorized response
    │←──────────────────────
    │
    ├─→ Clear localStorage
    │   localStorage.removeItem('roomsdekho:user')
    │
    ├─→ Redirect to login
    │   router.replace('/login?type=owner')
    │
    └─→ Show toast
        "Session expired. Please login again."

Network Error:
    │
    │ Network request failed
    ✗ No internet or server down
    │
    ├─→ Show toast
    │   "Network error. Please check your connection."
    │
    ├─→ Enable retry button
    │   [Retry]
    │
    └─→ Cache data if possible
        Show last known data with warning
```

---

**End of Visual Diagram** 🎉

This diagram shows the complete owner journey from login to property management and lead handling, including all API flows, state management, and error handling.
